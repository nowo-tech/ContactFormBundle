services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: '../src/'
        exclude:
            - '../src/Kernel.php'

    App\Service\DemoContactFormFileUploadHandler:
        arguments:
            $uploadDirectory: '%kernel.project_dir%/var/contact-uploads'
